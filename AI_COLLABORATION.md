# Codex·Claude 협업 가이드

## 1. 역할 분담

### Codex — 실행 담당

- 실제 코드와 공개 콘텐츠 수정
- 행사 기간·운영시간·관람료·할인·도슨트·주차·공식 링크의 현재 공식 출처 확인
- CSP, 개인정보, 모바일 표시 및 데이터 정합성 검증
- 관련 테스트와 정적 검사 실행
- 의도한 파일만 명시적으로 스테이징
- 사용자가 승인한 범위에서 커밋, PR, 병합 및 GitHub Pages 배포
- Claude의 리뷰 지적을 근거와 현재 코드에 대조한 뒤 선택적으로 반영

### Claude — 리뷰 담당

- 변경된 코드와 콘텐츠의 오류·누락·회귀 가능성 검토
- 한국어 문구의 가독성, 용어 통일, 사실과 의견의 구분 확인
- HTML, JavaScript, 공개 JSON 사이의 일정·상태·시간·장소 문구 일치 여부 확인
- 접근성, 모바일 사용성, 보안 및 개인정보 관점의 개선 제안
- 구현이나 배포가 필요한 항목을 Codex에게 명확하게 인계

Claude는 기본적으로 읽기 전용이다. 사용자가 Claude에게 별도로 구현을 요청하지 않는 한 파일과 Git 상태를 변경하지 않는다.

## 2. 기본 작업 흐름

1. 사용자가 목표와 배포 여부를 지정한다.
2. Codex가 공식 출처와 저장소 상태를 확인하고 변경을 구현한다.
3. Codex가 리뷰 대상 파일 목록과 기준 커밋, 미커밋 diff 상태를 알린 뒤 해당 파일 수정을 잠시 멈춘다.
4. Claude가 고정된 diff를 읽기 전용으로 검토한다.
5. Claude가 아래 리뷰 형식으로 결과를 전달한다.
6. Codex가 지적 사항을 재현·검증하고 필요한 항목만 수정한다.
7. Codex가 프로젝트 검증 명령을 실행하고 최종 diff와 포함 파일을 확인한다.
8. 사용자가 배포까지 요청한 경우에만 Codex가 커밋·PR·병합·배포하고 공개 페이지를 확인한다.

## 3. 리뷰 체크포인트

리뷰를 요청할 때 Codex는 다음 정보를 Claude에게 전달한다.

```text
목표:
리뷰 기준 커밋:
현재 브랜치:
리뷰 대상 파일:
의도적으로 제외한 파일:
확인한 공식 출처와 확인일:
실행한 검증과 결과:
특히 확인받고 싶은 부분:
```

같은 작업 폴더를 공유하는 동안에는 다음 규칙을 지킨다.

- Claude가 리뷰하는 동안 Codex는 리뷰 대상 파일을 추가 수정하지 않는다.
- Claude는 미커밋 변경을 stash, reset, checkout 또는 자동 포맷하지 않는다.
- 병렬 구현이 꼭 필요하면 같은 작업 폴더가 아니라 별도 Git worktree와 별도 브랜치를 사용한다.
- 다른 작업자의 변경이 섞여 있으면 파일별 소유 범위를 먼저 확인한다.

## 4. Claude 리뷰 결과 형식

```text
검토 결과: 승인 | 수정 권고 | 차단

발견사항
- [P1] 배포 전에 반드시 수정할 오류
  - 파일/위치:
  - 근거:
  - 영향:
  - 제안:
- [P2] 수정이 권장되는 오류 또는 정합성 문제
- [P3] 선택적 개선 제안

문구 검토
- 현재 문구:
- 제안 문구:
- 변경 이유:

일정 정합성
- HTML:
- JavaScript:
- 공개 JSON:
- 불일치 여부:

확인하지 못한 부분
- 외부 공식 출처 재확인이 필요한 내용
- 실행하지 않은 테스트
```

문제가 없으면 억지로 개선점을 만들지 말고, 검토한 범위와 남은 위험만 명시한다.

## 5. Codex 최종 검증

변경 범위에 맞춰 필요한 검증을 선택하되, 공개 일정과 추천 데이터 변경 시 기본적으로 다음을 확인한다.

```powershell
node --check exhibition_club_codex_package/public/app.js
node --check exhibition_club_codex_package/public/notice.js
node scripts/validate-weekly-digest.mjs
node scripts/send-telegram-update.js --dry-run
git diff --check
git status --short --branch
```

추가로 다음을 확인한다.

- CSP 위반이 되는 인라인 스크립트·스타일·외부 CDN이 없는지
- `weekly-digest.public.json`과 `notice.js`의 공개 대체 데이터가 일치하는지
- 실명, 연락처, 원문 대화 등 개인정보가 공개 파일에 포함되지 않았는지
- 캐시 버전이 수정된 CSS·JavaScript·JSON과 함께 갱신됐는지
- 커밋 대상에 `outputs/`, 원본 카카오톡 내보내기, 비밀키 또는 무관한 사용자 변경이 없는지

## 6. Git과 배포 책임

- Git 스테이징, 커밋, PR, 병합 및 배포는 Codex가 담당한다.
- Claude의 `승인`은 자동 배포 권한을 뜻하지 않는다.
- 사용자가 배포를 요청하지 않았다면 Codex도 로컬 수정과 검증까지만 수행한다.
- 보호된 `main`이 PR과 필수 검사를 요구하면 Codex는 해당 절차를 따른다.
- 배포 완료 보고에는 변경 요약, 검증 결과, 커밋 또는 PR, Actions 결과, 공개 URL을 포함한다.
